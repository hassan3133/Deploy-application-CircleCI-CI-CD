apiVersion: v1
kind: Service
metadata:
  name: {{ include "vs-dtuc-app.fullname" . }}
  labels:
    {{- include "vs-dtuc-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vs-dtuc-app.selectorLabels" . | nindent 4 }}
