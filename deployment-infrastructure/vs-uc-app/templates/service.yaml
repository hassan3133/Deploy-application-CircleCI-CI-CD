apiVersion: v1
kind: Service
metadata:
  name: {{ include "vs-uc-app.fullname" . }}
  labels:
    {{- include "vs-uc-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vs-uc-app.selectorLabels" . | nindent 4 }}
